<UserControl x:Class="Gita.Practice.App.Views.GroupPracticeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Gita.Practice.App.ViewModels"
             xmlns:vw="clr-namespace:Gita.Practice.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="250"
             d:DesignWidth="400">

    <!-- RESOURCES -->
    <UserControl.Resources>

        <!-- Converter -->
        <vw:WaitModeToVisibilityConverter x:Key="WaitModeToVisibilityConverter"/>

        <!-- Playback button brushes -->
        <LinearGradientBrush x:Key="PlaybackNormalBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF66A3FF" Offset="0"/>
            <GradientStop Color="#FF2D6BFF" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PlaybackHoverBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF6FB8FF" Offset="0"/>
            <GradientStop Color="#FF335EFF" Offset="1"/>
        </LinearGradientBrush>

        <!-- Playback Button Style -->
        <Style x:Key="PlaybackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PlaybackNormalBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="6,0" />
            <Setter Property="MinWidth" Value="72" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{StaticResource PlaybackHoverBrush}" />
                                <Setter TargetName="border" Property="Opacity" Value="0.98" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for Play button: disable when IsPlaying == true -->
        <Style x:Key="PlayButtonStyle" TargetType="Button" BasedOn="{StaticResource PlaybackButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Style for Pause/Stop buttons: disable when IsPlaying == false -->
        <Style x:Key="PauseStopButtonStyle" TargetType="Button" BasedOn="{StaticResource PlaybackButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPlaying}" Value="False">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LabelStyle">
            <Setter Property="Control.FontWeight" Value="Bold"/>
            <Setter Property="Control.VerticalAlignment" Value="Center"/>
            <Setter Property="Control.Margin" Value="0,0,8,0"/>
        </Style>

        <Style x:Key="GroupBoxStyle">
            <Setter Property="Control.Margin" Value="0,12,8,0"/>
            <Setter Property="Control.BorderBrush" Value="#800080"/>
            <Setter Property="Control.BorderThickness" Value="1"/>
            <Setter Property="Control.MinWidth" Value="400"/>
        </Style>

        <Style x:Key="GroupBoxHeaderStyle">
            <Setter Property="Control.FontWeight" Value="Bold"/>
            <Setter Property="Control.Foreground" Value="Blue"/>
        </Style>
    </UserControl.Resources>

    <!-- MAIN LAYOUT -->
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <!-- Side panel for progress -->
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Column="0" Margin="0,0,15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Input panel -->
                <RowDefinition Height="Auto"/>
                <!-- Playback buttons -->
                <RowDefinition Height="Auto"/>
                <!-- Help control -->
            </Grid.RowDefinitions>

        <!-- Hidden MediaElement -->
        <MediaElement Name="mediaElement"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Visibility="Collapsed"
                      Source="{Binding AudioSource}" />

        <!-- INPUT PANEL -->
        <Border Grid.Row="0"
                Background="#f5f7fa"
                CornerRadius="10"
                Padding="12"
                Margin="0,0,0,15"
                BorderBrush="#d0d7de"
                BorderThickness="1">

            <StackPanel>

                <!-- Updated grid: three columns so all inputs align to the same input column -->
                <Grid Margin="0,0,0,5" Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <!-- Label column -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Input column: fixed width so all controls align with the Participants TextBox -->
                        <ColumnDefinition Width="Auto" SharedSizeGroup="InputColumn"/>
                        <!-- Remainder column for spacing -->
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Participants grouped into "General Settings" -->
                    <GroupBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource GroupBoxStyle}">
                        <GroupBox.Header>
                            <TextBlock Text="General" Style="{StaticResource GroupBoxHeaderStyle}"/>
                        </GroupBox.Header>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="InputColumn"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Chapter:" Style="{StaticResource LabelStyle}" Margin="8,0,0,0"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedChapterName}" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Participants:" Style="{StaticResource LabelStyle}" Margin="8,0,0,0"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Width="100" HorizontalAlignment="Left"
                                     Text="{Binding NumberOfParticipants, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnValidationError=True}"
                                     >
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </GroupBox>

                    <!-- YOUR TURN GROUPBOX -->
                    <GroupBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource GroupBoxStyle}">
                        <GroupBox.Header>
                            <TextBlock Text="Your turn" Style="{StaticResource GroupBoxHeaderStyle}"/>
                        </GroupBox.Header>

                        <Grid Margin="8,6,8,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="InputColumn"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Your Turn -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Your Turn:" Margin="0,0,8,0" VerticalAlignment="Center" Style="{StaticResource LabelStyle}"/>
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="{Binding YourTurn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnValidationError=True}"
                                     Width="100"
                                     HorizontalAlignment="Left">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                            <!-- Wait Mode -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Wait Mode:" Style="{StaticResource LabelStyle}"/>

                            <!-- Use a horizontal StackPanel inside the input column so its controls align with other inputs -->
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" >
                                <!-- ComboBox aligned to input column -->
                                <ComboBox Width="100"
                                          ItemsSource="{Binding Source={x:Static local:WaitModeValues.All}}"
                                          SelectedItem="{Binding WaitMode}" />

                                <!-- Duration label -->
                                <TextBlock Text="Duration (sec):"
                                           Style="{StaticResource LabelStyle}"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding WaitMode, Converter={StaticResource WaitModeToVisibilityConverter}}"/>

                                <!-- Duration textbox -->
                                <TextBox Width="100"
                                         Text="{Binding YourDurationInSeconds}"
                                         VerticalAlignment="Center"
                                         Visibility="{Binding WaitMode, Converter={StaticResource WaitModeToVisibilityConverter}}"/>
                            </StackPanel>

                            <!-- Repeat Sloka -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Repeat Your Sloka:" VerticalAlignment="Center" Style="{StaticResource LabelStyle}"/>
                            <CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding RepeatYourSloka}" HorizontalAlignment="Left"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Grid.Row="4" Grid.ColumnSpan="2" Style="{StaticResource GroupBoxStyle}">
                        <GroupBox.Header>
                            <TextBlock Text="Other Participant(s)" Style="{StaticResource GroupBoxHeaderStyle}"/>
                        </GroupBox.Header>


                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Stanza Count -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"
                   Text="Number of Stangs:" />

                            <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding ParticipantStanzaCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, NotifyOnValidationError=True}"
                 Width="100"
                 HorizontalAlignment="Left"
                 Margin="0,0,0,8"/>

                            <!-- Playback Speed -->
                            <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Playback Speed:"
                                       Style="{StaticResource LabelStyle}" />

                            <StackPanel Grid.Row="1" Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,8">

                                <Slider Minimum="0.5"
                    Maximum="2.0"
                    TickFrequency="0.25"
                    IsSnapToTickEnabled="True"
                    Value="{Binding PlayingSpeed}"
                    Width="120"/>

                                <TextBlock Text="{Binding PlayingSpeed, StringFormat={}{0:0.00}x}"
                       VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>
            </StackPanel>
        </Border>

            <!-- PLAYBACK BUTTONS -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">

            <Button Style="{StaticResource PlayButtonStyle}"
                    Command="{Binding PlayCommand}"
                    ToolTip="Play">
                <TextBlock Text="Play" FontSize="12"/>
            </Button>

            <Button Style="{StaticResource PauseStopButtonStyle}"
                    Command="{Binding PauseCommand}"
                    ToolTip="Pause">
                <TextBlock Text="Pause" FontSize="12"/>
            </Button>

                <Button Style="{StaticResource PauseStopButtonStyle}"
                        Command="{Binding StopCommand}"
                        ToolTip="Stop">
                    <TextBlock Text="Stop" FontSize="12"/>
                </Button>

            </StackPanel>

        <!-- HELP CONTROL -->
        <vw:HelpControl Grid.Row="2" Margin="0,15,0,0"/>
        </Grid>

        <!-- PROGRESS SIDE PANEL -->
        <Border Grid.Column="1"
                Background="#f8f9fa"
                CornerRadius="10"
                Padding="15"
                BorderBrush="#d0d7de"
                BorderThickness="1"
                MinWidth="250"
                MaxWidth="300">
            <StackPanel>
                <TextBlock Text="Progress"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#2d3748"
                           Margin="0,0,0,15"/>

                <vw:GroupPracticeProgressView DataContext="{Binding ProgressViewModel}"/>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>