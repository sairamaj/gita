<UserControl x:Class="Gita.Practice.App.Views.ChapterTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gita.Practice.App.Views"
             xmlns:vm="clr-namespace:Gita.Practice.App.ViewModels"
             xmlns:svc="clr-namespace:Gita.Practice.App.Services"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Professional color scheme -->
        <SolidColorBrush x:Key="TreeViewBackground" Color="#F8F9FA"/>
        <SolidColorBrush x:Key="TreeViewBorder" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="SelectedBackground" Color="#E3F2FD"/>
        <SolidColorBrush x:Key="SelectedBorder" Color="#2196F3"/>
        <SolidColorBrush x:Key="HoverBackground" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="ChapterNumberForeground" Color="#1976D2"/>
        <SolidColorBrush x:Key="ChapterNameForeground" Color="#212121"/>
        <SolidColorBrush x:Key="ExpanderForeground" Color="#757575"/>
        
        <!-- Professional TreeViewItem Style -->
        <Style TargetType="TreeViewItem">
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="Padding" Value="6,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource ChapterNameForeground}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Selection indicator -->
                            <Border x:Name="SelectionBorder"
                                    Grid.ColumnSpan="2"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="3,0,0,0"
                                    SnapsToDevicePixels="True">
                                <Border x:Name="ContentBorder"
                                        Background="Transparent"
                                        CornerRadius="4"
                                        Padding="{TemplateBinding Padding}"
                                        Margin="2,0">
                                    <ContentPresenter x:Name="PART_Header"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Border>
                            </Border>
                            
                            <!-- Items container -->
                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Column="1"
                                            Margin="18,0,0,0"/>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <!-- Mouse Over -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource HoverBackground}"/>
                            </Trigger>
                            
                            <!-- Selected -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="{StaticResource SelectedBorder}"/>
                                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource SelectedBackground}"/>
                                <Setter Property="Foreground" Value="{StaticResource SelectedBorder}"/>
                            </Trigger>
                            
                            <!-- Selected and Mouse Over -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ContentBorder" Property="Background" Value="{StaticResource SelectedBackground}"/>
                                <Setter TargetName="ContentBorder" Property="Opacity" Value="0.95"/>
                            </MultiTrigger>
                            
                            <!-- Expanded -->
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource ChapterNumberForeground}"/>
                            </Trigger>
                            
                            <!-- Focused -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="{StaticResource SelectedBorder}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Style for the expander toggle button -->
        <Style TargetType="ToggleButton">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource ExpanderForeground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <Grid>
                                <Path x:Name="ExpandPath"
                                      Data="M 0 2 L 4 6 L 8 2 Z"
                                      Fill="{TemplateBinding Foreground}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RenderTransformOrigin="0.5,0.5"
                                      Stretch="Uniform">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ExpandPath"
                                                             Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                             To="90"
                                                             Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ExpandPath"
                                                             Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                             To="0"
                                                             Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource ChapterNumberForeground}"/>
                                <Setter TargetName="ExpandPath" Property="Fill" Value="{StaticResource ChapterNumberForeground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header Section -->
        <Border Grid.Row="0"
                Background="{StaticResource ChapterNumberForeground}"
                Padding="12,10"
                BorderBrush="{StaticResource TreeViewBorder}"
                BorderThickness="0,0,0,1">
            <TextBlock Text="Chapters"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </Border>
        
        <!-- TreeView Container -->
        <Border Grid.Row="1"
                Background="{StaticResource TreeViewBackground}"
                BorderBrush="{StaticResource TreeViewBorder}"
                BorderThickness="0,0,1,0"
                CornerRadius="0">
            <TreeView x:Name="ChaptersTreeView"
                      ItemsSource="{Binding Chapters}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      BorderThickness="0"
                      Padding="8,8,8,8"
                      svc:TreeViewSelectionService.SelectedItemChangedCommand="{Binding ChapterSelectedCommand}">
                
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}"/>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type vm:ChapterViewModel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Chapter Number Badge -->
                            <Border Grid.Column="0"
                                    Background="{StaticResource ChapterNumberForeground}"
                                    CornerRadius="12"
                                    Width="24"
                                    Height="24"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding Number}"
                                           Foreground="White"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Chapter Name -->
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Name}"
                                       FontSize="14"
                                       FontWeight="Normal"
                                       Foreground="{StaticResource ChapterNameForeground}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
