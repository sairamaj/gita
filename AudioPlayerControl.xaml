<UserControl x:Class="Gita.Practice.App.Views.AudioPlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Gita.Practice.App.ViewModels"
             xmlns:vw="clr-namespace:Gita.Practice.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="250"
             d:DesignWidth="400">

	<!-- RESOURCES -->
	<UserControl.Resources>

		<!-- Converter -->
		<vw:WaitModeToVisibilityConverter x:Key="WaitModeToVisibilityConverter"/>

		<!-- Playback button brushes -->
		<LinearGradientBrush x:Key="PlaybackNormalBrush" StartPoint="0,0" EndPoint="0,1">
			<GradientStop Color="#FF66A3FF" Offset="0"/>
			<GradientStop Color="#FF2D6BFF" Offset="1"/>
		</LinearGradientBrush>

		<LinearGradientBrush x:Key="PlaybackHoverBrush" StartPoint="0,0" EndPoint="0,1">
			<GradientStop Color="#FF6FB8FF" Offset="0"/>
			<GradientStop Color="#FF335EFF" Offset="1"/>
		</LinearGradientBrush>

		<!-- Playback Button Style -->
		<Style x:Key="PlaybackButtonStyle" TargetType="Button">
			<Setter Property="Background" Value="{StaticResource PlaybackNormalBrush}" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="Padding" Value="10,6" />
			<Setter Property="Margin" Value="6,0" />
			<Setter Property="MinWidth" Value="72" />
			<Setter Property="MinHeight" Value="44" />
			<Setter Property="FontWeight" Value="SemiBold" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="HorizontalContentAlignment" Value="Center" />
			<Setter Property="VerticalContentAlignment" Value="Center" />

			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">

							<ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
						</Border>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background"
                                        Value="{StaticResource PlaybackHoverBrush}" />
								<Setter TargetName="border" Property="Opacity" Value="0.98" />
							</Trigger>

							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="border" Property="Opacity" Value="0.9" />
							</Trigger>

							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="border" Property="Opacity" Value="0.45" />
							</Trigger>
						</ControlTemplate.Triggers>

					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

	</UserControl.Resources>

	<!-- MAIN LAYOUT -->
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<!-- Input panel -->
			<RowDefinition Height="Auto"/>
			<!-- Playback buttons -->
		</Grid.RowDefinitions>

		<!-- Hidden MediaElement -->
		<MediaElement Name="mediaElement"
                      LoadedBehavior="Manual"
                      UnloadedBehavior="Stop"
                      Visibility="Collapsed"
                      Source="{Binding AudioSource}" />

		<!-- INPUT PANEL -->
		<Border Grid.Row="0"
                Background="#f5f7fa"
                CornerRadius="10"
                Padding="12"
                Margin="0,0,0,15"
                BorderBrush="#d0d7de"
                BorderThickness="1">

			<StackPanel>

				<TextBlock Text="Session Settings"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#333"
                           Margin="0,0,0,10"/>

				<!-- Updated grid: three columns so all inputs align to the same input column -->
				<Grid Margin="0,0,0,10" Grid.IsSharedSizeScope="True">
					<Grid.ColumnDefinitions>
						<!-- Label column -->
						<ColumnDefinition Width="Auto"/>
						<!-- Input column: fixed width so all controls align with the Participants TextBox -->
						<ColumnDefinition Width="Auto" SharedSizeGroup="InputColumn"/>
						<!-- Remainder column for spacing -->
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<!-- Participants -->
					<TextBlock Grid.Row="0" Grid.Column="0" Text="Participants:" Margin="0,0,16,8"/>
					<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NumberOfParticipants}" Width="100" HorizontalAlignment="Left" Margin="0,0,0,8"/>

					<!-- Your Turn -->
					<TextBlock Grid.Row="1" Grid.Column="0" Text="Your Turn:" Margin="0,0,16,8"/>
					<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding YourTurn}" Width="100" HorizontalAlignment="Left" Margin="0,0,0,8"/>

					<!-- Wait Mode -->
					<TextBlock Grid.Row="2" Grid.Column="0" Text="Wait Mode:" Margin="0,0,16,8"/>

					<!-- Use a horizontal StackPanel inside the input column so its controls align with other inputs -->
					<StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,8" >
						<!-- ComboBox aligned to input column -->
						<ComboBox Width="100"
                                  ItemsSource="{Binding Source={x:Static local:WaitModeValues.All}}"
                                  SelectedItem="{Binding WaitMode}" />

						<!-- Duration label -->
						<TextBlock Text="Duration (sec):"
                                   Margin="16,0,4,0"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding WaitMode, Converter={StaticResource WaitModeToVisibilityConverter}}"/>

						<!-- Duration textbox -->
						<TextBox Width="100"
                                 Text="{Binding YourDurationInSeconds}"
                                 VerticalAlignment="Center"
                                 Visibility="{Binding WaitMode, Converter={StaticResource WaitModeToVisibilityConverter}}"/>
					</StackPanel>

					<!-- Repeat Sloka -->
					<TextBlock Grid.Row="3" Grid.Column="0" Text="Repeat Your Sloka:" Margin="0,0,16,8"/>
					<CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding RepeatYourSloka}" HorizontalAlignment="Left" Margin="0,0,0,8"/>

					<GroupBox Header="Stangs & Playback Settings" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,10,0,0">
						<Grid Margin="10">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>

							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>

							<!-- Stangs Count -->
							<TextBlock Grid.Row="0" Grid.Column="0"
									   Text="Number of Stangs count:"
									   Margin="0,0,16,8"/>

							<TextBox Grid.Row="0" Grid.Column="1"
									 Text="{Binding ParticipantEntryCount}"
									 Width="100"
									 HorizontalAlignment="Left"
									 Margin="0,0,0,8"/>

							<!-- Playback Speed -->
							<TextBlock Grid.Row="1" Grid.Column="0"
									   Text="Playback Speed:"
									   Margin="0,0,16,8"/>

							<StackPanel Grid.Row="1" Grid.Column="1"
										Orientation="Horizontal"
										HorizontalAlignment="Left"
										Margin="0,0,0,8">

								<Slider Minimum="0.5"
										Maximum="2.0"
										TickFrequency="0.25"
										IsSnapToTickEnabled="True"
										Value="{Binding PlayingSpeed}"
										Width="120"/>

								<TextBlock Text="{Binding PlayingSpeed, StringFormat={}{0:0.00}x}"
										   Margin="8,0,0,0"
										   VerticalAlignment="Center"/>
							</StackPanel>
						</Grid>
					</GroupBox>

				</Grid>
			</StackPanel>
		</Border>

		<!-- PLAYBACK BUTTONS -->
		<StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">

			<Button Style="{StaticResource PlaybackButtonStyle}"
                    Command="{Binding PlayCommand}"
                    ToolTip="Play">
				<TextBlock Text="Play" FontSize="12"/>
			</Button>

			<Button Style="{StaticResource PlaybackButtonStyle}"
                    Command="{Binding PauseCommand}"
                    ToolTip="Pause">
				<TextBlock Text="Pause" FontSize="12"/>
			</Button>

			<Button Style="{StaticResource PlaybackButtonStyle}"
                    Command="{Binding StopCommand}"
                    ToolTip="Stop">
				<TextBlock Text="Stop" FontSize="12"/>
			</Button>

		</StackPanel>

	</Grid>
</UserControl>